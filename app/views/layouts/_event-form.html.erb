<%= form_for(@event) do |f| %>
  <%= f.label :date %>
  <%= f.date_select :date, placeholder: "mm/dd/yyyy", class: "form-control" %>

  <%= f.label :description %>
  <%= f.text_field :description,class: "form-control" %><br>

  <%= f.submit yield(:button_text), class: 'btn btn-primary' %>
<% end %>