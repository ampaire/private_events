<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <% provide(:button_text, 'Create an Event') %>
      <div class="create">
        <h1><%= @user.name %></h1>
        <div class= "pull-right">
        <%= link_to 'Create a new event', new_event_path %>
        </div>
      </div>
    <%= render 'events/upcoming' %>
  </div>
</div>