<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <h1>Create a new event, <%= @user.name %></h1><br>
    <% provide(:button_text, 'Create') %>
    <%= render 'layouts/event-form', url: new_event_path(@event) %>
  <div>
</div>